public class alumnosdeclase
{
   String salonDeClase;
   public alumnosdeclase(){
       char [] est = new char[10];
       for(int i = 0; i< est.length; i++){
           est[i] = (char)('est' + i);
        }
       for(char est; estudia
       
    }
}
